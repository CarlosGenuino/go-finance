version: "1"
project:
    id: "go-finance"
packages:
  - name: "db"
    queries: "./db/query/"
    path: "./db/sqlc/"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: true
    emit_interface: true
    emit_exact_table_names: false
    emit_empty_slices: true
